import { SliceProductPipe } from './slice-product.pipe';

describe('SliceProductPipe', () => {
  it('create an instance', () => {
    const pipe = new SliceProductPipe();
    expect(pipe).toBeTruthy();
  });
});
